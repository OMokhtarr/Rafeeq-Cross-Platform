<?xml version="1.0" encoding="utf-8"?>
<!--
  Night variant of one prayer card: light text on a dark card. Name over time,
  and nothing else — the timer lives on the strip beside the card, not inside
  it, so the card stays the compact block the design calls for.

  Every view id here must also exist in layout/widget_prayer_card.xml —
  PrayerDeckFactory sets views by id and a mismatch crashes whichever
  configuration is inflated.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/card_root"
    android:layout_width="@dimen/widget_card_width"
    android:layout_height="@dimen/widget_card_height"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_card_background_dark"
    android:paddingStart="@dimen/widget_card_padding_h"
    android:paddingEnd="@dimen/widget_card_padding_h"
    android:paddingTop="@dimen/widget_card_padding_v"
    android:paddingBottom="@dimen/widget_card_padding_v">

    <TextView
        android:id="@+id/card_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:includeFontPadding="false"
        android:textSize="17sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textAlignment="center" />

    <TextView
        android:id="@+id/card_time"
        android:layout_marginTop="2dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:includeFontPadding="false"
        android:textSize="17sp"
        android:maxLines="1"
        android:textAlignment="center" />

</LinearLayout>
