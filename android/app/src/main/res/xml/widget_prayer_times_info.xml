<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is 0: Android's own widget-update period is capped at 30
  minutes and would fire pointlessly. The prayer alarms drive refreshes
  instead (PrayerWidgetProvider.refresh), which is exactly when the display
  changes, plus a midnight roll for the new day. The per-second countdown is
  not an app refresh at all — it is a Chronometer ticked by the system inside
  the launcher process.

  4x1: a strip, not a timetable. Resizing is horizontal-only — the deck's
  cards do not reflow usefully into a taller widget, and a 4x1 has no
  meaningful taller form.

  autoAdvanceViewId is deliberately unset: the deck advances when the user
  swipes it, never on a timer.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:resizeMode="horizontal"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_prayer_times"
    android:previewImage="@mipmap/ic_launcher"
    android:previewLayout="@layout/widget_prayer_times_preview"
    android:description="@string/prayer_widget_desc_strip"
    android:updatePeriodMillis="0" />
